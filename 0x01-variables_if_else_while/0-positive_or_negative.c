# include <stdio.h>
#include <time.h>
#include <stdio.h>

/**
 * main - assigns a random number to int n everytime
 * it executes, and prints it
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2; main(void){
int n;
if( n>o){
	printf("is positive\n");
}
else if (n==o){
	printf("is zero\n");
}
else{
	printf("is negative\n");
}
return(0);
}
